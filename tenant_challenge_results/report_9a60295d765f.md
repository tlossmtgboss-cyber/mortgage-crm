# Perennia AI — Multi-Tenant SaaS Readiness Report
**Run ID:** 9a60295d765f
**Date:** 2026-02-21T09:56:19.172163+00:00
**Duration:** 1.3s

---

## Overall Assessment

| Metric | Value |
|--------|-------|
| **Overall Score** | **0.0%** |
| **Status** | **NOT READY — Significant gaps remain** |
| **SaaS Ready** | **NO ❌** |
| Checks Passed | 0/87 |
| Blockers Failed | 0 |

## Domain Scores

| Domain | Score | Passed | Failed | Blockers |
|--------|-------|--------|--------|----------|
| isolation | 0.0% | 0/18 | 0 | 0 |
| licensing | 0.0% | 0/12 | 0 | 0 |
| provisioning | 0.0% | 0/10 | 0 | 0 |
| ai_isolation | 0.0% | 0/11 | 0 | 0 |
| performance | 0.0% | 0/10 | 0 | 0 |
| whitelabel | 0.0% | 0/8 | 0 | 0 |
| metering | 0.0% | 0/10 | 0 | 0 |
| compliance | 0.0% | 0/8 | 0 | 0 |

## Remediation Plan (Priority Order)

### 1. 🔴 ISO-001: RLS policies exist on all tenant-scoped tables
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** All tenant-scoped tables have RLS policies defined in pg_policies

**Fix:**
```
CREATE POLICY tenant_isolation ON {table}
  USING (org_id = current_setting('app.current_org_id')::UUID);
CREATE POLICY tenant_insert ON {table}
  FOR INSERT WITH CHECK (org_id = current_setting('app.current_org_id')::UUID);
```

### 2. 🔴 ISO-002: RLS is ENABLED (not just policies created)
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** relrowsecurity = true for all tenant-scoped tables

**Fix:**
```
ALTER TABLE {table} ENABLE ROW LEVEL SECURITY;
```

### 3. 🔴 ISO-003: Cross-tenant SELECT returns zero rows
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** Zero rows returned when querying another tenant's data

**Fix:**
```
Fix RLS policy — ensure USING clause checks current_setting('app.current_org_id')
```

### 4. 🔴 ISO-004: Cross-tenant INSERT is blocked
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** INSERT rejected or org_id silently overridden to current tenant

**Fix:**
```
Add WITH CHECK to INSERT policy that enforces org_id = current_org_id
```

### 5. 🔴 ISO-005: Cross-tenant UPDATE is blocked
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** Zero rows affected when updating another tenant's data

**Fix:**
```
Ensure UPDATE policy uses USING clause with org_id check
```

### 6. 🔴 ISO-006: Cross-tenant DELETE is blocked
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** Zero rows affected when deleting another tenant's data

**Fix:**
```
Ensure DELETE policy uses USING clause with org_id check
```

### 7. 🔴 ISO-007: API endpoints enforce tenant scope
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** 403 Forbidden or empty result set on every endpoint

**Fix:**
```
Add middleware that sets tenant context from JWT:
  db.execute(text("SET LOCAL app.current_org_id = :org_id"), {"org_id": user.org_id})
Ensure ALL endpoints pass through this middleware.
```

### 8. 🔴 ISO-008: IDOR protection on all resource endpoints
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** No resource leakage when enumerating IDs

**Fix:**
```
All resource lookups must include org_id in WHERE clause (defense-in-depth over RLS)
```

### 9. 🔴 ISO-009: Bulk/export endpoints respect tenant boundary
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** Exported data contains ONLY current tenant's records

**Fix:**
```
All export queries must be tenant-scoped; verify output before delivery
```

### 10. 🔴 ISO-010: Search/filter cannot cross tenant boundary
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** Search results contain only current tenant's data

**Fix:**
```
All search queries must include tenant_id filter; vector search must filter by tenant
```

### 11. 🔴 ISO-011: File storage paths include tenant prefix
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** All upload/download code paths include tenant_id in storage key

**Fix:**
```
# Every file operation must include tenant scope:
key = f"{tenant_id}/{document_type}/{file_id}/{filename}"
# NEVER store files at root level without tenant prefix
```

### 12. 🔴 ISO-012: Presigned URLs scoped to tenant
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** Access denied when using Tenant A's presigned URL from Tenant B session

**Fix:**
```
Validate tenant ownership before generating presigned URLs; add tenant check on download endpoint
```

### 13. 🔴 ISO-013: Document download verifies tenant ownership
**Domain:** isolation | **Severity:** BLOCKER
**Pass Criteria:** 403 when requesting another tenant's document

**Fix:**
```
Add tenant_id check in document download handler BEFORE streaming file
```

### 14. 🔴 AIC-001: Agent system prompts scoped to tenant
**Domain:** ai_isolation | **Severity:** BLOCKER
**Pass Criteria:** Prompt construction code only injects current tenant's data

**Fix:**
```
# System prompt builder MUST:
# 1. Load org config: SELECT * FROM org_settings WHERE org_id = current_org_id()
# 2. Load org-specific agent config (custom instructions, tone, restrictions)
# 3. NEVER include data from other orgs
# 4. Include org name, branding in agent persona
```

### 15. 🔴 AIC-002: RAG/vector search scoped to tenant
**Domain:** ai_isolation | **Severity:** BLOCKER
**Pass Criteria:** Vector search filtered by tenant_id metadata; zero cross-tenant results

**Fix:**
```
# Pinecone/pgvector queries MUST include tenant filter:
results = index.query(
    vector=query_embedding,
    filter={"org_id": {"$eq": current_org_id}},
    top_k=10
)
# NEVER query without tenant filter
```

### 16. 🔴 AIC-003: Conversation history isolated per tenant
**Domain:** ai_isolation | **Severity:** BLOCKER
**Pass Criteria:** Tenant A cannot load Tenant B's conversation history

**Fix:**
```
conversation_messages table must have org_id column with RLS policy
```

### 17. 🔴 AIC-004: Tool execution scoped to tenant
**Domain:** ai_isolation | **Severity:** BLOCKER
**Pass Criteria:** All tool functions enforce tenant_id; return only current tenant's data

**Fix:**
```
# Every tool function signature must include org_id:
async def get_pipeline_metrics(org_id: str, user_id: str, **kwargs):
    return await db.fetch("SELECT * FROM loans WHERE org_id = :org_id", {"org_id": org_id})
# Tool orchestrator passes org_id from authenticated session
```

### 18. 🔴 AIC-009: Prompt injection cannot access cross-tenant data
**Domain:** ai_isolation | **Severity:** BLOCKER
**Pass Criteria:** Injection attempts return normal refusal; no cross-tenant data exposed

**Fix:**
```
# Defense layers:
# 1. RLS at DB level (even if prompt tricks tool into wrong query)
# 2. Tool functions hardcode org_id from session (ignore user-provided org_id)
# 3. Output sanitization checks for foreign org_ids before responding
```

### 19. 🟠 ISO-014: Redis/cache keys include tenant prefix
**Domain:** isolation | **Severity:** CRITICAL
**Pass Criteria:** All cache get/set operations include tenant_id in key

**Fix:**
```
# Every cache key must be prefixed:
cache_key = f"tenant:{tenant_id}:{resource_type}:{resource_id}"
# NEVER use generic keys like "pipeline_stats" without tenant scope
```

### 20. 🟠 ISO-015: Cache invalidation is tenant-scoped
**Domain:** isolation | **Severity:** CRITICAL
**Pass Criteria:** Tenant B's cache entries survive Tenant A's cache flush

**Fix:**
```
Use tenant-prefixed keys; flush only keys matching tenant:{tenant_id}:*
```

---
*© 2026 TL Development LLC — Perennia AI Platform*