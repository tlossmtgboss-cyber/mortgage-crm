import React from 'react';

/**
 * SchedulingAlerts — displays scheduling conflict/capacity alerts on the Calendar page.
 * Placeholder until full implementation is wired up.
 */
export default function SchedulingAlerts() {
  return null;
}
