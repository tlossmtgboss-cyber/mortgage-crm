# Procfile for Railway deployment
# Runs migrations before starting the application
# Note: Railway provides PORT env variable that must be used

web: sh -c "python run_migrations.py && uvicorn main:app --host 0.0.0.0 --port \${PORT:-8080}"
