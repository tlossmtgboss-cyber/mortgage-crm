"""Agent Challenge CI integration package.

Thin wrapper around backend.agents.tools.u_agent_challenge that exposes
a deterministic CLI entrypoint for nightly regression testing and per-PR
gating. The underlying challenge framework is left untouched.
"""
